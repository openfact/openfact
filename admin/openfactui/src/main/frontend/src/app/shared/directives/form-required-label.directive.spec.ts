/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FormRequiredLabelDirective } from './form-required-label.directive';

describe('FormRequiredLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new FormRequiredLabelDirective();
    expect(directive).toBeTruthy();
  });
});
