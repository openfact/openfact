language: java
sudo: false
install: true
dist: trusty
cache:
  directories:
  - $HOME/.m2/repository
  - $HOME/.sonar/cache
jdk:
- oraclejdk8
addons:
  sonarqube:
    token: $SONAR_TOKEN
script:
  - sonar-scanner
notifications:
  email: false
